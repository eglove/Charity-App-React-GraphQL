schema {
    query: Query
}

type Query {
    allRecurringDonations:[RecurringDonation]
    recurringDonation(id: String):RecurringDonation
}

type RecurringDonation {
    id: String
    userId: String
    favoriteId: String
    donationAmount: Float
}
